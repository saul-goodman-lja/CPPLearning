#include <iostream>
#include <vector>
#include <ranges>
#include <algorithm>

using std::cin, std::cout, std::endl;
using std::vector;

class Solution{
public:
    int lower_bound1(vector<int> &nums, int target){

    }
};
void test0(){
    vector<int> a;
    std::ranges::lower_bound(a.begin() ,a.end());
}

int main(void){

    test0();

    return 0;
}